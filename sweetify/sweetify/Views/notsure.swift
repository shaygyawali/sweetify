//
//  notsure.swift
//  sweetify
//
//  Created by Shay Gyawali on 7/21/24.
//

import Foundation
/*Picker("Detail", selection: $selectedDetail){
    ForEach(Detail.allCases) {detail in
        Text("\(detail.rawValue.capitalized)")
            .frame(height: 100)
    }
}
.frame(width: UIScreen.main.bounds.width*0.9)
.pickerStyle(.segmented)
if selectedDetail == .ingredients {
if let ingredients = viewModel.detail?.ingredients {
    List(ingredients.indices, id: \.self) { index in
        HStack {
            Text(viewModel.detail?.ingredients[index] ?? "Ingredient Holder")
            Spacer()
            Text(viewModel.detail?.measures[index] ?? "X")
        }
    }
    .scrollContentBackground(.hidden)
} else {
    Text("🫙 No Ingredients Found")
}

}
else if selectedDetail == .instructions {
if let instructions = viewModel.detail?.instructions {
    let instructionsList = viewModel.listifyInstructions(instructions: instructions)
    List(instructionsList.indices, id: \.self) { index in
        Text("\(String(index + 1)). \(instructionsList[index])")
    }
    .scrollContentBackground(.hidden)

}
}
Spacer()
}*/
